# Cloudflare Pages custom headers.
#
# /fonts/* are immutable by convention: the woff2 filenames change if
# the faces ever do (see src/styles/fonts.css), so a year-long cache
# is safe. Overrides the Pages default of max-age=14400.

/fonts/*
  Cache-Control: public, max-age=31536000, immutable
