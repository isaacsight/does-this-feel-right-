# provenance-substrate has moved

Canonical home: **https://github.com/isaacsight/provenance-substrate**

```bash
pip install provenance-substrate
```

The Python package (`provsubstrate`, CLI `provsub`) — the kbot-finance ledger
substrate ported to Python and generalized to Lean proofs, manuscript claims,
paper reproductions, a quantitative-hallucination benchmark, and peer-review
provenance — was extracted from this monorepo in PR #74 (2026-08-17) via
`git subtree split` with history preserved, so the thing people star is one
idea. Issues and PRs go there. This directory is a pointer only.

The TypeScript reference and the origin of the finance v1 conformance vectors
remain in [`packages/kbot-finance/`](../kbot-finance) (mirrored to
[`isaacsight/kbot-finance`](https://github.com/isaacsight/kbot-finance)).
